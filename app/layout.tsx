import './globals.css'

import Helmet from '@/app/components/helmet'

import { Poppins } from 'next/font/google'
import { Dela_Gothic_One } from 'next/font/google'
const poppins = Poppins({ weight: ['400'], subsets: ['latin'] })

export const metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
}

// This does nothing except slap a helmet on the layout and load the other pages
export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="table m-auto">
      <body className={poppins.className + " table-cell align-middle"}>
        <div className="h-full mx-auto p-0">
          <Helmet />
          <div className="h-full w-full">
            {children}
          </div>
        </div>
      </body>
    </html>
  )
}
